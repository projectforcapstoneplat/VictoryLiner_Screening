// Shared between JobPostingForm.jsx (create/edit) and HrDashboard.jsx
// (filtering) so the two never drift out of sync with each other.
export const STATIONS = ['Cubao', 'Caloocan', 'Baguio'];
export const EMPLOYMENT_TYPES = ['Full-time', 'Contractual'];
